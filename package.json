{
  "name": "stop-and-learn",
  "private": true,
  "version": "1.0.0",
  "description": "Stop & Learn deployed on Vercel with Firebase Auth, Firestore and admin APIs.",
  "scripts": {
    "start": "python3 -m http.server 4173 --bind 127.0.0.1",
    "check:api": "node --check api/config.js && node --check api/_lib/firebase-admin.js && node --check api/admin/users.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  }
}
